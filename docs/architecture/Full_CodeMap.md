# Full Code Map

## Root Directory

- **`app.py`**: The core backend application. Contains variable `tasks` for in-memory state, `split_audio` for logic, and API routes (`/api/upload`, `/api/status`, `/api/test-connection`).
- **`requirements.txt`**: Python dependencies.
- **`README.md`**: Project documentation.
- **`AGENTS.md`**: Agent SOP and project management workflow.

## Static Assets (`static/`)

- **`index.html`**: Main UI.
- **`style.css`**: Styling.
- **`script.js`**: Frontend logic (upload handling, polling, UI updates).

## Documentation (`docs_local/`)

- **`resources.md`**: List of free resources and API providers.

## Architecture Documentation (`docs/architecture/`)

- _(Planned)_: Detailed architectural diagrams or specific module docs.
